# Contributing to dsh-skill-router

- 先搜索已有 issue，避免重复。
- 提交前运行 `npm run typecheck` 与 `npm test`。
- 修改 UI/源码后更新 `lib/`，保证 clone 后可直接安装/测试。
- 不要把个人开关状态、token、密钥、本机绝对路径提交进仓库。
- 涉及 DSH 插件升级/热更，遵循 `dsh-optimization-consensus`。
